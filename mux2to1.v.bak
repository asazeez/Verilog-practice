module mux2to1 (
	i_a,
	i_b,
	i_s,
	o_f);

input i_a;
input i_b;
input i_s;

output o_s;

wire temp_f;

@always (o_s)
	begin 
	if (o_s) temp_f = i_a;
	else temp_f = i_b;
	end

assign f=temp_f;
endmodule
	